include ../shared/head.pug
body
    include ../shared/navigation.pug
    main
        if products.length > 0
            each product in products
                ul
                    li #{product.title}
                    li #{product.price}
                    